{
  "name": "adhubkenya",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client-react"
  ],
  "scripts": {
    "dev": "npm --workspace=client-react run dev",
    "build": "npm --workspace=client-react run build",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
